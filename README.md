# beadle

Читалка задач [beads](https://github.com/gastownhall/beads) для человека. `bd` владеет
данными, beadle показывает их: список, доска, эпики.

Главное отличие от `beads-ui`, Beadbox и perles: **активный проект живёт в адресе
страницы.** Две вкладки с разными проектами не переключают друг друга, ссылкой на
отфильтрованную доску можно поделиться.

## Как запустить

```bash
pnpm install
bun apps/server/src/index.ts add ~/путь/к/проекту
pnpm dev
```

Сервер сам добавит в реестр проект, из каталога которого его запустили.

## Устройство

```
apps/server/     Hono на bun; данные — только через CLI bd
apps/web/        Vite + React + TanStack Router, стили на vanilla-extract
packages/protocol/  Zod-схемы, выведенные из живого вывода bd list --json
```

Правила работы — `CLAUDE.md`. Наблюдения по каркасу `../mxerf-claude` — `docs/canon-notes.md`.

## Что дальше

- [x] каркас монорепо, проверки, хуки
- [x] контракт `protocol` по живым данным
- [x] сервер: реестр воркспейсов, чтение и фильтрация задач, расчёт заблокированных
- [ ] фронт: маршруты с воркспейсом в пути, список задач
- [ ] доска и эпики
- [ ] живое обновление при изменениях в `.beads`
- [ ] смена статуса и приоритета через `bd`
- [ ] сборка в один бинарь через `bun build --compile`
- [ ] стиль по дизайн-системе v3

Репозиторий локальный, без remote.
