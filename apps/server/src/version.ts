/**
 * Версия beadle. Живёт здесь, а не в контракте: показывает её сейчас только
 * тот, кто запускается, — CLI и бинарь. Понадобится экрану — переедет
 * в `packages/protocol`, а пока лишний общий модуль заводить не за чем.
 *
 * Расхождение с корневым `package.json` ловит тест рядом: держать номер
 * в двух местах можно, полагаться на внимательность — нет.
 */
export const VERSION = '0.2.0'
