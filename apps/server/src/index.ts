import { main } from './cli.ts'

/**
 * Точка входа разработки: тот же CLI, но без вшитого фронта — его в этом
 * режиме раздаёт vite на своём порту и проксирует сюда `/api`.
 */
main(process.argv.slice(2))
